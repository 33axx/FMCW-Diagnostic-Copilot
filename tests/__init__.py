# FMCW Diagnostic Agent — Test Suite

import sys
from pathlib import Path

# Add project root to PYTHONPATH so imports work from any directory
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
